<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid.Resources>
        <SolidColorBrush x:Key="T" Color="#2B2B2B"/>
        <SolidColorBrush x:Key="Gold" Color="#B2996B"/>
        <SolidColorBrush x:Key="Neutral" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="NeutralText" Color="#333333"/>
        <SolidColorBrush x:Key="NeutralBorder" Color="#D1D5DB"/>
        
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gold}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Style="{StaticResource Card}">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top" Margin="2,0,0,6" FontWeight="Bold" 
                                         Foreground="{StaticResource T}" Text="{TemplateBinding Header}"/>
                                <ContentPresenter Margin="0"/>
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>
        
        <Style x:Key="BtnGold" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Gold}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A08D61"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="BtnSecondaryWhite" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8F8F8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        
        <!-- Selection buttons: white, no gold border, left-aligned -->
        <Style x:Key="BtnSelectionNeutral" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8F8F8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Grid.Resources>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>  <!-- Filters + Settings -->
        <RowDefinition Height="*"/>     <!-- Views -->
        <RowDefinition Height="Auto"/>  <!-- Selection -->
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="12"/>
        <ColumnDefinition Width="520"/>
    </Grid.ColumnDefinitions>

    <!-- Filters -->
    <GroupBox Header="Filters" Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,0,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Parameter:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbP" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Waarde:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbV" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Zoek:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtS" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Stretch"/>
        </Grid>
    </GroupBox>

    <!-- Instellingen dupliceren (rechts) -->
    <GroupBox Header="Instellingen dupliceren" Grid.Row="0" Grid.Column="2" Margin="0,0,0,8" VerticalAlignment="Top">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Viewtemplate:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbTpl" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Scope Box:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbScp" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>
            <Label Grid.Row="2" Grid.Column="0" Content="Dupliceer:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbDup" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>
        </Grid>
    </GroupBox>

    <!-- Rechts: naamgeving / kopieën / set -->
    <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top">
        <GroupBox Header="Naamgeving" Margin="0,8,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Prefix:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtPre" Grid.Row="0" Grid.Column="1"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Find:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtFind" Grid.Row="1" Grid.Column="1"/>
                <Label Grid.Row="2" Grid.Column="0" Content="Replace:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtRepl" Grid.Row="2" Grid.Column="1"/>
                <Label Grid.Row="3" Grid.Column="0" Content="Suffix:" VerticalAlignment="Center"/>
                <Grid Grid.Row="3" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtOld" Grid.Column="0"/>
                    <TextBlock Grid.Column="1" Text="–" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <TextBox x:Name="txtNew" Grid.Column="2"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- KOPIEËN: alleen Aantal -->
        <GroupBox Header="Kopieën" Margin="0,8,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Aantal kopieën:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtCount" Grid.Row="0" Grid.Column="1" Text="1"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Set opslaan" Margin="0,8,0,0" VerticalAlignment="Bottom" Padding="8,8,8,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Setnaam:" VerticalAlignment="Center" Padding="2,0,8,0" Height="26"/>
                <TextBox x:Name="txtSet" Grid.Row="0" Grid.Column="1" VerticalContentAlignment="Center"/>
                <Button x:Name="btnSaveSet" Grid.Row="1" Grid.Column="1" Content="Opslaan (aangevinkte views)" Style="{StaticResource BtnGold}" HorizontalAlignment="Right"/>
            </Grid>
        </GroupBox>
    </StackPanel>

    <!-- Views -->
    <GroupBox Header="Views" Grid.Row="1" Grid.Column="0" Margin="0,8,0,8">
        <ListBox x:Name="lst" MinHeight="440"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling"
             ScrollViewer.CanContentScroll="True"
             SelectionMode="Extended"
             BorderBrush="#D1D5DB"
             BorderThickness="1"
             Background="White"/>
    </GroupBox>

    <!-- Selectie (left-aligned, simple buttons, no gold border) -->
    <GroupBox Header="Selectie" Grid.Row="2" Grid.Column="0" Margin="0,4,0,0">
        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnAll" Content="Alles" Style="{StaticResource BtnSelectionNeutral}" Margin="0,0,8,0"/>
            <Button x:Name="btnNone" Content="Geen" Style="{StaticResource BtnSelectionNeutral}"/>
        </StackPanel>
    </GroupBox>
</Grid>
